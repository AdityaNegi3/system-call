import React from "react";
import { createRoot } from "react-dom/client";
import SecureSyscall from "../secure_syscall (1).jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SecureSyscall />
  </React.StrictMode>
);
